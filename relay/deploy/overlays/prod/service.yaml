apiVersion: v1
kind: Service
metadata:
  name: relay-service
selector:
  app: relay
spec:
  selector:
    app: relay
  ports:
    - protocol: TCP
      port: 80
      targetPort: 2700
  type: LoadBalancer